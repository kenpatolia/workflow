cd /
cd /Users/kenpatolia/workflow/workflow/dockerdemo

docker build -t drupal  .
docker run  -d --name drupal -p 8080:80 -v  /Users/kenpatolia/workflow/workflow/dockerdemo/drupal:/var/www/ drupal
//docker run -d  -p8080:80 httpd
