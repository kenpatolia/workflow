cd /
cd /Users/kenpatolia/workflow/workflow/dockerdemo

docker build -t hello-world .
docker run  -d --name helloworld -p 8082:80 -v  /Users/kenpatolia/workflow/workflow/dockerdemo/src:/var/www/html hello-world
//docker run -d  -p8080:80 httpd
